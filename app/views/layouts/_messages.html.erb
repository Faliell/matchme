<div id="message-<%= message.id %>">

  <% if message.sender?(current_user) %>
  <div class="d-flex">
  <%= cl_image_tag current_user.photo.key, class: "avatar_2"%>
    <strong class="align-self-center extrabold ps-2"><%= message.user.first_name %></strong>
    <i class="timestamp align-self-center d-inline ps-5"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </div>
  <% else %>
  <%= cl_image_tag message.user.photo.key, class: "avatar_2"%>
    <strong class="align-self-center extrabold ps-2"><%= message.user.first_name %></strong>
    <i class="timestamp-match align-self-center d-inline ps-5"><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  <% end %>
  <p class="content pt-3"><%= message.content %></p>

</div>
